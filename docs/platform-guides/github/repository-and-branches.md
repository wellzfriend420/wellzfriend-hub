# リポジトリとブランチ

- アプリごとに独立したGitHubリポジトリを使用し、Hubへソースコードを置かない。
- 既定ブランチは `main` とし、直接変更を禁止する保護ルールを設定する。
- 作業ブランチは短命とし、`feature/説明`、`fix/説明`、`docs/説明` を基本とする。
- リポジトリの所有役割、可視性、アーカイブ状態を[リポジトリ台帳](../../../registries/repositories/README.md)で管理する。
- 削除ではなく原則アーカイブし、移管時はリンクと責任役割を更新する。
